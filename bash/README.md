# Bash usefule command

### Debian - Update single package
```console
bugli@local:~$ apt-get install --only-upgrade <packagename>
```

### Debian - Add repository to apt list

```console
$ add-apt-repository "deb <repository> stretch main contrib non-free"
```
